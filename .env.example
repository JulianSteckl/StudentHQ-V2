# MongoDB Atlas connection string (required for cloud sync API routes)
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/scholar?retryWrites=true&w=majority

# Google OAuth 2.0 client ID (public; used by API to verify Bearer tokens)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Optional: extra browser origins allowed to call /api/* (comma-separated)
# ALLOWED_ORIGINS=https://studenthq-v2.vercel.app,http://localhost:5173
